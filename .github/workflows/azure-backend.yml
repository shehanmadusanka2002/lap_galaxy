name: Deploy Backend to Azure Container Apps (Docker Hub)

on:
  push:
    branches: [ "main" ]
    paths: [ 'backend/**' ]
  workflow_dispatch:

jobs:
  build-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven

    - name: Build with Maven
      working-directory: ./backend
      run: mvn clean package -DskipTests

    # --- වෙනස් වුන කොටස 1: Docker Hub එකට Login වීම ---
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # --- වෙනස් වුන කොටස 2: Docker Hub එකට Image එක Push කිරීම ---
    - name: Build and Push Docker Image to Docker Hub
      working-directory: ./backend
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/lapgalaxy-backend:${{ github.sha }} .
        docker build -t ${{ secrets.DOCKER_USERNAME }}/lapgalaxy-backend:latest .
        docker push ${{ secrets.DOCKER_USERNAME }}/lapgalaxy-backend:${{ github.sha }}
        docker push ${{ secrets.DOCKER_USERNAME }}/lapgalaxy-backend:latest

    # --- Azure Login (Deploy කරන්න විතරක් ඕන) ---
    - name: Login to Azure
      uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}

    # --- වෙනස් වුන කොටස 3: Deploy කිරීම (Image එක ගන්නේ Docker Hub එකෙන්) ---
    - name: Deploy to Azure Container Apps
      uses: azure/container-apps-deploy-action@v2
      with:
        imageToDeploy: ${{ secrets.DOCKER_USERNAME }}/lapgalaxy-backend:${{ github.sha }}
        targetPort: 8080
        ingress: external
        # කලින් හරියටම හැදුන Resource Group එකේ නම Secret එකට දාන්න
        resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }} 
        containerAppName: lapgalaxy-backend
        # location එක ඕන නම් විතරක් දාන්න, නැත්නම් Auto ගනීවි
        # location: australiaeast 
        environmentVariables: |
          DB_URL=secretref:db-url
          DB_USERNAME=secretref:db-username
          DB_PASSWORD=secretref:db-password